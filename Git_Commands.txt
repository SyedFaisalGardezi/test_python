
https://codemy.com/git


$ git config --global user.name "Syed Faisal Ishtiaq"
$ git config --global user.email "faisal.gerdazi@gmail.com"
$ git config --global push.default matching
$ git config --global alias.co checkout
$ git init


$ git add . ## add the files to git, temp location

$ git commit -am "initial commit"    ## it adds the files to local repository

## after modification need to add that again to repo
$ git add Git_Commands.txt
$ git commit -m Git_Commands.txt

now move back to orignal /default path to add SSH keys

$cd ~\

$ pwd
c/users/syed faisal
